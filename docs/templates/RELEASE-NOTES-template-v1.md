---
id: "RN-YYYY-NNN"
version: "{vX.Y.Z}"
status: "{草稿 / 评审中 / 已批准 / 已归档}"
owner: "{负责人}"
---

# {版本号} 发布说明

> **文档编号**：`RN-YYYY-NNN`  
> **版本**：`{vX.Y.Z}`  
> **模板版本**：`v1`  
> **状态**：`{草稿 / 评审中 / 已批准 / 已归档}`  
> **编写人/适用对象**：`{姓名}`  
> **编写日期**：`{YYYY-MM-DD}`  
> **关联文档**：  
> - `docs/CHANGELOG-vX.Y.Z.md`  
> - `docs/PRD-vX.Y.Z.md`  
> - `docs/TDD-vX.Y.Z.md`  
> - `docs/DEPLOYMENT-LOG-vX.Y.Z.md`  
> **评审人**：`{产品负责人、技术负责人、市场负责人}`  
> **发布日期**：`{YYYY-MM-DD}`  
> **范围**：`{产品名}`  
> **发布状态（RELEASE-NOTES 专用）**：`{草稿 / 已发布}`

---

## 1. 发布概述

### 1.1 版本摘要

用 2-3 句话概括本次发布的核心价值：

- `{核心价值 1}`
- `{核心价值 2}`

### 1.2 发布范围

| 类型 | 内容 |
|------|------|
| 新增功能 | `{FR-001、FR-002}` |
| 优化改进 | `{性能、体验}` |
| 缺陷修复 | `{BUG-001、BUG-002}` |
| 破坏性变更 | `{如有}` |

---

## 2. 新功能

### 2.1 {功能名称}

- **需求来源**：`{FR-xxx / 用户反馈}`
- **功能描述**：`{做了什么}`
- **用户价值**：`{解决了什么问题}`
- **使用路径**：`{菜单 / 链接 / 命令}`
- **截图 / 演示**：`{可选}`

### 2.2 {功能名称}

...

---

## 3. 优化与改进

- `{优化项 1：简要描述}`
- `{优化项 2：简要描述}`

---

## 4. 缺陷修复

| 缺陷编号 | 描述 | 影响范围 |
|----------|------|----------|
| `BUG-001` | `{问题描述}` | `{模块}` |
| `BUG-002` | `{问题描述}` | `{模块}` |

---

## 5. 安全更新

> 本次发布包含的安全修复、漏洞披露或安全策略变更。

| 安全编号 | CVE / 内部编号 | 描述 | 影响范围 |
|----------|----------------|------|----------|
| `SEC-001` | `{CVE-XXXX-XXXXX}` | `{修复描述}` | `{模块}` |

> 若无安全更新，保留本节并填写“无”。

---

## 6. 破坏性变更

> 若本次发布包含破坏性变更，必须在此明确列出并给出迁移步骤。

| 变更项 | 影响范围 | 迁移方案 |
|--------|----------|----------|
| `{变更 1}` | `{API / UI / 数据库}` | `{迁移步骤}` |

---

## 7. 依赖与兼容性

- **最低客户端版本**：`{vX.Y.Z}`
- **依赖服务版本**：`{服务名 vX.Y.Z}`
- **浏览器支持**：`{Chrome 120+ / Safari 17+}`
- **移动端支持**：`{iOS 16+ / Android 13+}`

---

## 8. 已知问题

| 问题 | 影响 | 规避方案 | 计划修复版本 |
|------|------|----------|--------------|
| `{问题 1}` | `{低/中/高}` | `{如何规避}` | `{vX.Y.Z}` |

---

## 9. 升级说明

### 9.1 服务端升级

```bash
# {示例命令}
```

### 9.2 客户端升级

```bash
# {示例命令}
```

### 9.3 数据库迁移

```bash
# {迁移脚本或命令}
```

---

## 10. 相关链接

- PRD：`{docs/PRD-vX.Y.Z.md}`
- TDD：`{docs/TDD-vX.Y.Z.md}`
- 测试报告：`{docs/QA-TEST-REPORT-vX.Y.Z.md}`
- 部署记录：`{docs/DEPLOYMENT-LOG-vX.Y.Z.md}`
- 回滚方案：`{docs/ROLLBACK-PLAN-vX.Y.Z.md}`

---

## 11. 检查清单

- [ ] 所有 `{占位符}` 已替换为实际内容
- [ ] 功能列表与 PRD 一致
- [ ] 缺陷编号与 issue tracker 一致
- [ ] 安全更新已评估并记录
- [ ] 破坏性变更已通知受影响方
- [ ] 升级说明已验证
- [ ] 回滚方案已准备
