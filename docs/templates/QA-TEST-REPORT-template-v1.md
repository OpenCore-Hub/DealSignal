---
id: "QTR-YYYY-NNN"
version: "{vX.Y.Z}"
status: "{草稿 / 评审中 / 已批准 / 已归档}"
owner: "{负责人}"
---

# {版本号} 测试报告

> **文档编号**：`QTR-YYYY-NNN`  
> **版本**：`{vX.Y.Z}`  
> **模板版本**：`v1`  
> **状态**：`{草稿 / 评审中 / 已批准 / 已归档}`  
> **编写人/适用对象**：`{QA 负责人 / 测试工程师}`  
> **编写日期**：`{YYYY-MM-DD}`  
> **关联文档**：  
> - `docs/QA-TEST-PLAN-vX.Y.Z.md`  
> - `docs/PRD-vX.Y.Z.md`  
> - `docs/TDD-vX.Y.Z.md`  
> - `docs/RELEASE-NOTES-vX.Y.Z.md`  
> **评审人**：`{QA 负责人、技术负责人、产品负责人}`  
> **报告结论（QA-TEST-REPORT 专用）**：`{通过 / 有条件通过 / 不通过}`

---

## 1. 测试概览

| 项目 | 内容 |
|------|------|
| 测试版本 | `{vX.Y.Z}` |
| 测试范围 | `{功能 / 回归 / 性能 / 安全}` |
| 测试环境 | `{预发 / 生产灰度}` |
| 测试周期 | `{YYYY-MM-DD} ~ {YYYY-MM-DD}` |
| 测试负责人 | `{姓名}` |
| 总体结论 | `{通过 / 有条件通过 / 不通过}` |

---

## 2. 测试统计

| 测试类型 | 用例总数 | 通过 | 失败 | 跳过 | 通过率 |
|----------|----------|------|------|------|--------|
| 单元测试 | `{N}` | `{N}` | `{N}` | `{N}` | `{N%}` |
| 集成测试 | `{N}` | `{N}` | `{N}` | `{N}` | `{N%}` |
| E2E 测试 | `{N}` | `{N}` | `{N}` | `{N}` | `{N%}` |
| 性能测试 | `{N}` | `{N}` | `{N}` | `{N}` | `{N%}` |
| 安全测试 | `{N}` | `{N}` | `{N}` | `{N}` | `{N%}` |

---

## 3. 缺陷统计

| 严重级别 | 数量 | 已修复 | 未修复 | 备注 |
|----------|------|--------|--------|------|
| P0 | `{N}` | `{N}` | `{N}` | `{阻塞发布}` |
| P1 | `{N}` | `{N}` | `{N}` | `{需评估}` |
| P2 | `{N}` | `{N}` | `{N}` | `{可延期}` |
| P3 | `{N}` | `{N}` | `{N}` | `{不影响发布}` |

---

## 4. 未修复缺陷

| 缺陷编号 | 描述 | 严重级别 | 不修复原因 | 规避方案 | 计划版本 |
|----------|------|----------|------------|----------|----------|
| `BUG-001` | `{描述}` | `P2` | `{原因}` | `{方案}` | `vX.Y.Z` |

---

## 5. 性能测试结果

| 场景 | 目标 | 实际 | 结论 |
|------|------|------|------|
| `{P99 延迟}` | `< 500ms` | `{X ms}` | 通过/失败 |
| `{吞吐量}` | `> 1000 TPS` | `{X TPS}` | 通过/失败 |

---

## 6. 安全测试结果

| 检查项 | 工具/方式 | 结果 | 备注 |
|--------|-----------|------|------|
| 越权访问 | 手工 + 自动化 | 通过/失败 | |
| SQL 注入 | 自动化扫描 | 通过/失败 | |
| XSS | 自动化扫描 | 通过/失败 | |

---

## 7. 发布建议

- [ ] 满足发布条件，建议发布
- [ ] 有条件通过，需满足 {条件} 后发布
- [ ] 不满足发布条件，建议延期

---

## 8. 检查清单

- [ ] 测试用例覆盖所有 P0/P1 需求
- [ ] 缺陷已全部评估并记录
- [ ] 性能/安全测试结果已确认
- [ ] 报告已与开发/产品/运维同步
