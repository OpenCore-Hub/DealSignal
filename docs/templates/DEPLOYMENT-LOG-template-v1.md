---
id: "DL-YYYY-NNN"
version: "{vX.Y.Z}"
status: "{草稿 / 评审中 / 已批准 / 已归档}"
owner: "{负责人}"
---

# {版本号} 部署记录

> **文档编号**：`DL-YYYY-NNN`  
> **版本**：`{vX.Y.Z}`  
> **模板版本**：`v1`  
> **状态**：`{草稿 / 评审中 / 已批准 / 已归档}`  
> **编写人/适用对象**：`{DevOps / SRE / 发布负责人}`  
> **编写日期**：`{YYYY-MM-DD}`  
> **关联文档**：  
> - `docs/RELEASE-NOTES-vX.Y.Z.md`  
> - `docs/ROLLBACK-PLAN-vX.Y.Z.md`  
> - `docs/QA-TEST-REPORT-vX.Y.Z.md`  
> **评审人**：`{发布负责人、技术负责人}`  
> **部署日期**：`{YYYY-MM-DD HH:MM UTC+8}`  
> **部署状态（DEPLOYMENT-LOG 专用）**：`{计划中 / 已部署 / 已回滚}`

---

## 1. 部署信息

| 项目 | 内容 |
|------|------|
| 版本号 | `{vX.Y.Z}` |
| 部署环境 | `{生产 / 预发 / 灰度}` |
| 部署方式 | `{蓝绿 / 滚动 / 金丝雀}` |
| 影响范围 | `{全部用户 / 部分用户 / 特定区域}` |
| 发布负责人 | `{姓名}` |
| 技术负责人 | `{姓名}` |
| 产品负责人 | `{姓名}` |

---

## 2. 部署内容

- 本次部署包含的 PR / Commit：`{PR #123, #124}`
- 关联 Release Notes：`{docs/RELEASE-NOTES-vX.Y.Z.md}`
- 关联回滚方案：`{docs/ROLLBACK-PLAN-vX.Y.Z.md}`

---

## 3. 部署前检查

- [ ] 所有 P0/P1 测试用例通过
- [ ] 安全扫描无高危漏洞
- [ ] 数据库 migration 已在预发验证
- [ ] 配置变更已 review
- [ ] 回滚方案已准备
- [ ] 监控告警已配置
- [ ] 值班人员已确认

---

## 4. 部署步骤

| 步骤 | 时间 | 操作 | 执行人 | 结果 |
|------|------|------|--------|------|
| 1 | `{HH:MM}` | `{合并发布分支}` | `{姓名}` | 通过 |
| 2 | `{HH:MM}` | `{执行 migration}` | `{姓名}` | 通过 |
| 3 | `{HH:MM}` | `{部署 API Service}` | `{姓名}` | 通过 |
| 4 | `{HH:MM}` | `{部署 Web App}` | `{姓名}` | 通过 |
| 5 | `{HH:MM}` | `{灰度切流 5%}` | `{姓名}` | 通过 |
| 6 | `{HH:MM}` | `{全量切流}` | `{姓名}` | 通过 |

---

## 5. 部署后验证

- [ ] 健康检查：`{curl / 命令}`
- [ ] 核心 E2E 用例：`{命令}`
- [ ] 错误率 / 延迟指标正常
- [ ] 业务核心指标正常
- [ ] 用户反馈渠道无异常激增

---

## 6. 问题与处理

| 时间 | 问题 | 影响 | 处理措施 | 状态 |
|------|------|------|----------|------|
| `{HH:MM}` | `{问题描述}` | `{低/中/高}` | `{处理措施}` | `{已解决 / 观察中}` |

---

## 7. 后续事项

- [ ] 观察窗口结束（{X 小时}后）
- [ ] 更新监控阈值（如需要）
- [ ] 更新 Runbook（如需要）
- [ ] 发送发布通知

---

## 8. 检查清单

- [ ] 部署步骤已记录
- [ ] 部署前检查全部通过
- [ ] 部署后验证全部通过
- [ ] 问题已记录并跟踪
- [ ] 回滚方案未被触发或已成功演练
