---
id: "RBK-YYYY-NNN"
version: "{vX.Y.Z}"
status: "{草稿 / 评审中 / 已批准 / 已归档}"
owner: "{负责人}"
---

# {服务/组件名} 运维手册（Runbook）

> **文档编号**：`RBK-YYYY-NNN`  
> **版本**：`{vX.Y.Z}`  
> **模板版本**：`v1`  
> **状态**：`{草稿 / 评审中 / 已批准 / 已归档}`  
> **编写人/适用对象**：`{姓名}`  
> **编写日期**：`{YYYY-MM-DD}`  
> **关联文档**：  
> - `docs/ARCHITECTURE-vX.Y.Z.md`  
> - `docs/TDD-vX.Y.Z.md`  
> - `docs/RELEASE-NOTES-vX.Y.Z.md`  
> **评审人**：`{SRE 负责人、技术负责人}`  
> **适用范围**：`{生产 / 预发 / 全部}`

---

## 1. 服务概述

### 1.1 服务职责

`{该服务/组件的核心功能}`

### 1.2 架构位置

```text
{简单架构位置描述，可链接到 ARCHITECTURE 文档}
```

### 1.3 关键依赖

| 依赖 | 类型 | 影响 |
|------|------|------|
| `{PostgreSQL}` | 数据库 | `{服务不可用}` |
| `{Redis}` | 缓存 | `{性能下降}` |
| `{对象存储}` | 存储 | `{文件读写失败}` |

---

## 2. 日常运维

### 2.1 健康检查

```bash
# 健康检查端点
curl -s https://{service}/health

# 预期输出
{"status":"ok","version":"vX.Y.Z"}
```

### 2.2 关键指标

| 指标 | 正常范围 | 告警阈值 | 查看方式 |
|------|----------|----------|----------|
| CPU | `< 70%` | `> 80%` | `{Grafana 链接}` |
| 内存 | `< 80%` | `> 90%` | `{Grafana 链接}` |
| P99 延迟 | `< 500ms` | `> 1s` | `{Grafana 链接}` |
| 错误率 | `< 0.1%` | `> 1%` | `{Grafana 链接}` |

### 2.3 日志查询

```bash
# {常用日志查询命令}
```

---

## 3. 启动与停止

### 3.1 启动

```bash
# {启动命令}
```

### 3.2 优雅停止

```bash
# {停止命令}
```

### 3.3 重启

```bash
# {重启命令}
```

---

## 4. 扩容与缩容

### 4.1 垂直扩容

```bash
# {命令}
```

### 4.2 水平扩容

```bash
# {命令}
```

---

## 5. 常见故障处理

### 5.1 {故障场景 1}

**现象**：`{描述}`

**影响**：`{用户/功能}`

**诊断步骤**：

1. `{查看指标}`
2. `{查看日志}`

**处理步骤**：

1. `{步骤 1}`
2. `{步骤 2}`

**回滚方式**：`{如何回滚}`

**验证方式**：`{如何确认恢复}`

### 5.2 {故障场景 2}

...

---

## 6. 备份与恢复

### 6.1 备份策略

| 数据 | 备份方式 | 频率 | 保留周期 |
|------|----------|------|----------|
| `{数据库}` | `{物理/逻辑备份}` | `{每日}` | `{30 天}` |
| `{对象存储}` | `{跨区复制}` | `{实时}` | `{90 天}` |

### 6.2 恢复步骤

```bash
# {恢复命令}
```

---

## 7. 变更管理

- 所有配置变更需走 `{变更流程}`。
- 变更前必须在 `{预发环境}` 验证。
- 变更后需观察 `{X 分钟}` 关键指标。

---

## 8. 联系人与升级

| 角色 | 联系方式 | 升级条件 |
|------|----------|----------|
| 值班工程师 | `{电话/IM}` | 任何 P1 事件 |
| Tech Lead | `{电话/IM}` | P0 或 15 分钟未恢复 |
| 安全负责人 | `{电话/IM}` | 安全相关事件 |

---

## 9. 相关链接

- 架构文档：`{docs/ARCHITECTURE-vX.Y.Z.md}`
- TDD：`{docs/TDD-vX.Y.Z.md}`
- 监控大屏：`{链接}`
- 告警规则：`{链接}`
- 部署脚本：`{scripts/...}`

---

## 10. 检查清单

- [ ] 所有 `{占位符}` 已替换为实际内容
- [ ] 健康检查命令已验证
- [ ] 关键指标和告警阈值已确认
- [ ] 常见故障场景至少覆盖 TOP 3
- [ ] 备份与恢复步骤已演练
- [ ] 联系人信息准确可用
