---
id: "INC-YYYYMMDD-NNN"
version: "{vX.Y.Z}"
status: "{草稿 / 评审中 / 已批准 / 已归档}"
owner: "{负责人}"
linked_docs:
  - "{docs/PRD-vX.Y.Z.md}"
  - "{docs/TDD-vX.Y.Z.md}"
  - "{docs/ADR-vX.Y.Z.md}"
  - "{docs/RUNBOOK-vX.Y.Z.md}"
downstream_docs_to_update:
  - "{docs/RUNBOOK-vX.Y.Z.md}"
  - "{docs/THREAT-MODEL-vX.Y.Z.md}"
  - "{docs/SLO-SERVICE-LEVEL-vX.Y.Z.md}"
---

# {事件编号} 事件响应报告

> **文档编号**：`INC-YYYYMMDD-NNN`  
> **版本**：`{vX.Y.Z}`  
> **模板版本**：`v1`  
> **状态**：`{草稿 / 评审中 / 已批准 / 已归档}`  
> **编写人/适用对象**：`{事件指挥官 / SRE / 值班工程师}`  
> **编写日期**：`{YYYY-MM-DD}`  
> **关联文档**：  
> - `docs/ROLLBACK-PLAN-vX.Y.Z.md`  
> - `docs/RELEASE-NOTES-vX.Y.Z.md`  
> - `docs/PRD-vX.Y.Z.md`  
> - `docs/TDD-vX.Y.Z.md`  
> - `docs/ADR-vX.Y.Z.md`  
> - `docs/RUNBOOK-vX.Y.Z.md`  
> **评审人**：`{技术负责人、SRE 负责人、产品负责人}`  
> **事件状态（INCIDENT 专用）**：`{检测中 / 处理中 / 已缓解 / 已恢复 / 已复盘}`  
> **事件标题**：`{一句话描述}`  
> **严重级别**：`{P0 / P1 / P2 / P3}`  
> **开始时间**：`{YYYY-MM-DD HH:MM UTC+8}`  
> **发现时间**：`{YYYY-MM-DD HH:MM UTC+8}`  
> **恢复时间**：`{YYYY-MM-DD HH:MM UTC+8}`  
> **持续时间**：`{X 小时 Y 分钟}`  
> **值班工程师**：`{姓名}`  
> **事件指挥官**：`{姓名}`

---

## 1. 事件摘要

用 3-5 句话说明：发生了什么、影响范围、最终结果。

---

## 2. 影响范围

| 维度 | 影响 |
|------|------|
| 服务/模块 | `{API / Web / 上传 / 解析 / 通知}` |
| 用户影响 | `{X% 用户 / Y 个租户}` |
| 功能影响 | `{登录失败 / 上传失败 / 查看失败}` |
| 数据影响 | `{数据丢失 / 数据不一致 / 无影响}` |
| 财务/合规影响 | `{如有}` |

---

## 3. 时间线

| 时间（UTC+8） | 事件 | 操作人 |
|---------------|------|--------|
| `{HH:MM}` | `{检测到告警}` | `{系统/值班}` |
| `{HH:MM}` | `{确认事件}` | `{值班工程师}` |
| `{HH:MM}` | `{启动响应}` | `{事件指挥官}` |
| `{HH:MM}` | `{定位根因}` | `{工程师}` |
| `{HH:MM}` | `{实施缓解}` | `{工程师}` |
| `{HH:MM}` | `{服务恢复}` | `{工程师}` |
| `{HH:MM}` | `{事件关闭}` | `{事件指挥官}` |

---

## 4. 根因分析

### 4.1 直接原因

`{导致事件的最直接技术或操作原因}`

### 4.2 根本原因（5 Whys）

1. `{为什么发生？}`
2. `{为什么没有被阻止？}`
3. `{为什么监控没发现？}`
4. `{为什么恢复慢？}`
5. `{为什么流程没起作用？}`

### 4.3 触发条件

`{什么操作、流量、配置或依赖变更触发了事件}`

---

## 5. 处理过程

### 5.1 诊断步骤

1. `{查看日志 / 指标 / 链路追踪}`
2. `{复现 / 缩小范围}`
3. `{定位到具体组件}`

### 5.2 缓解措施

| 措施 | 时间 | 效果 |
|------|------|------|
| `{措施 1}` | `{HH:MM}` | `{恢复 X% 流量}` |

### 5.3 恢复措施

| 措施 | 时间 | 效果 |
|------|------|------|
| `{措施 1}` | `{HH:MM}` | `{服务完全恢复}` |

---

## 6. 经验教训

### 6.1 做得好的

- `{团队协作迅速}`
- `{回滚及时}`

### 6.2 做得不足的

- `{告警噪声过多}`
- `{文档缺失}`

---

## 7. 后续行动项

| 编号 | 行动项 | 负责人 | 截止日期 | 状态 |
|------|--------|--------|----------|------|
| `ACTION-001` | `{修复根因}` | `{姓名}` | `{YYYY-MM-DD}` | 待办 |
| `ACTION-002` | `{增加监控/告警}` | `{姓名}` | `{YYYY-MM-DD}` | 待办 |
| `ACTION-003` | `{更新 Runbook}` | `{姓名}` | `{YYYY-MM-DD}` | 待办 |

---

## 8. 相关链接

- 告警：`{链接}`
- 日志：`{链接}`
- 链路追踪：`{链接}`
- 相关 PR / Commit：`{链接}`
- 相关 Runbook：`{docs/RUNBOOK-xxx.md}`
- 复盘会议录制：`{链接}`

---

## 9. 下游文档闭环更新

事件关闭后，必须评估并更新以下文档，确保知识沉淀到主链路：

| 下游文档 | 是否需更新 | 更新内容摘要 | 负责人 | 状态 |
|----------|------------|--------------|--------|------|
| `docs/RUNBOOK-vX.Y.Z.md` | {是 / 否} | {更新监控/应急步骤} | {姓名} | {待办 / 已完成} |
| `docs/THREAT-MODEL-vX.Y.Z.md` | {是 / 否} | {新增威胁或缓解措施} | {姓名} | {待办 / 已完成} |
| `docs/SLO-SERVICE-LEVEL-vX.Y.Z.md` | {是 / 否} | {调整 SLO/SLI} | {姓名} | {待办 / 已完成} |
| `docs/TDD-vX.Y.Z.md` | {是 / 否} | {补充架构约束或降级策略} | {姓名} | {待办 / 已完成} |
| `docs/ADR-vX.Y.Z.md` | {是 / 否} | {记录因事件触发的架构决策} | {姓名} | {待办 / 已完成} |
| `docs/PRD-vX.Y.Z.md` | {是 / 否} | {更新非功能需求或容错设计} | {姓名} | {待办 / 已完成} |

---

## 10. 检查清单

- [ ] 事件时间线完整
- [ ] 根因已定位并记录
- [ ] 影响范围已量化
- [ ] 后续行动项已分配并跟踪
- [ ] 相关 Runbook 已更新
- [ ] 复盘会议已完成
- [ ] 下游文档闭环更新已评估并执行
