---
id: "CHG-YYYY-NNN"
version: "{vX.Y.Z}"
status: "{草稿 / 评审中 / 已批准 / 已归档}"
owner: "{负责人}"
linked_docs:
  - "{docs/PRD-vX.Y.Z.md}"
  - "{docs/TDD-vX.Y.Z.md}"
  - "{docs/ADR-vX.Y.Z.md}"
  - "{docs/API-SPEC-vX.Y.Z.md}"
downstream_docs_to_update:
  - "{docs/PRD-vX.Y.Z.md}"
  - "{docs/TDD-vX.Y.Z.md}"
  - "{docs/API-SPEC-vX.Y.Z.md}"
  - "{docs/RELEASE-NOTES-vX.Y.Z.md}"
  - "{docs/CHANGELOG-vX.Y.Z.md}"
---

# {变更请求标题}

> **文档编号**：`CHG-YYYY-NNN`  
> **版本**：`{v0.1.0}`  
> **模板版本**：`v1`  
> **状态**：`{草稿 / 评审中 / 已批准 / 已拒绝 / 已实施}`  
> **编写人**：`{提出人}`  
> **提出日期**：`{YYYY-MM-DD}`  
> **关联基线文档**：
> - `docs/PRD-vX.Y.Z.md`
> - `docs/TDD-vX.Y.Z.md`
> - `docs/ADR-vX.Y.Z.md`
> - `docs/API-SPEC-vX.Y.Z.md`
> **评审人**：`{产品负责人、技术负责人、测试负责人}`

---

## 1. 变更背景与动机

### 1.1 触发原因

- `{描述导致变更的问题、机会或外部约束}`
- `{如：用户反馈、合规要求、技术债务、性能瓶颈}`

### 1.2 不变更的影响

| 维度 | 影响描述 |
|------|----------|
| 业务 | `{不实施变更对业务目标的影响}` |
| 技术 | `{对系统稳定性、可维护性、安全性的潜在风险}` |
| 合规 | `{是否会导致合规差距或审计问题}` |

### 1.3 预期收益

- `{变更成功后将带来的可量化收益}`
- `{如：降低延迟 X%、减少客诉 Y%、满足 Z 合规要求}`

---

## 2. 变更范围与影响

### 2.1 变更范围

| 范围项 | 包含 | 不包含 |
|--------|------|--------|
| 功能 | `{本次变更涉及的功能模块}` | `{明确排除的功能}` |
| 接口 | `{新增 / 修改 / 废弃的 API}` | `{保持不变的接口}` |
| 数据 | `{schema 变更、迁移、清洗}` | `{不涉及的数据}` |
| 配置 | `{新增或修改的配置项}` | `{默认配置}` |
| 文档 | `{需更新的 PRD/TDD/API 文档}` | `{无需更新的文档}` |

### 2.2 影响分析

| 影响对象 | 影响程度 | 说明 |
|----------|----------|------|
| 用户侧 | `{高 / 中 / 低}` | `{对用户操作流程、界面、权限的影响}` |
| 技术侧 | `{高 / 中 / 低}` | `{对架构、依赖、性能的影响}` |
| 运营侧 | `{高 / 中 / 低}` | `{对客服、培训、上线沟通的影响}` |
| 合规侧 | `{高 / 中 / 低}` | `{对隐私、安全、审计的影响}` |

---

## 3. 方案对比

> 至少提供 2 个可选方案，并从业务、技术、风险、成本等维度进行对比。

### 3.1 方案 A：{方案名称}

- **描述**：`{简要说明该方案的做法}`
- **优点**：`{...}`
- **缺点**：`{...}`
- **适用场景**：`{...}`

### 3.2 方案 B：{方案名称}

- **描述**：`{简要说明该方案的做法}`
- **优点**：`{...}`
- **缺点**：`{...}`
- **适用场景**：`{...}`

### 3.3 方案对比表

| 维度 | 方案 A | 方案 B | 推荐方案 |
|------|--------|--------|----------|
| 实施成本 | `{高 / 中 / 低}` | `{高 / 中 / 低}` | `{A / B}` |
| 时间进度 | `{周期}` | `{周期}` | `{A / B}` |
| 技术风险 | `{高 / 中 / 低}` | `{高 / 中 / 低}` | `{A / B}` |
| 用户体验 | `{影响}` | `{影响}` | `{A / B}` |
| 可回滚性 | `{易 / 中 / 难}` | `{易 / 中 / 难}` | `{A / B}` |
| 长期可维护性 | `{优 / 良 / 差}` | `{优 / 良 / 差}` | `{A / B}` |

**推荐方案**：`{A / B / 其他}`，理由：`{一句话说明}`

---

## 4. 成本 / 进度 / 风险影响

### 4.1 成本影响

| 成本类型 | 预估投入 | 说明 |
|----------|----------|------|
| 研发成本 | `{X 人天}` | `{前后端、测试、设计}` |
| 第三方成本 | `{金额 / 无}` | `{如新增 SaaS、云服务、审计费用}` |
| 机会成本 | `{说明}` | `{因本次变更而延期的其他事项}` |

### 4.2 进度影响

| 里程碑 | 原计划 | 变更后 | 偏差 |
|--------|--------|--------|------|
| `{里程碑 1}` | `{YYYY-MM-DD}` | `{YYYY-MM-DD}` | `{+X 天}` |
| `{里程碑 2}` | `{YYYY-MM-DD}` | `{YYYY-MM-DD}` | `{+X 天}` |

### 4.3 风险影响

| 风险项 | 可能性 | 影响 | 缓解措施 | 负责人 |
|--------|--------|------|----------|--------|
| `{如：范围蔓延}` | `{高 / 中 / 低}` | `{高 / 中 / 低}` | `{明确范围、分阶段实施}` | `{姓名}` |
| `{如：回归缺陷}` | `{高 / 中 / 低}` | `{高 / 中 / 低}` | `{增加回归测试、灰度发布}` | `{姓名}` |
| `{如：数据不一致}` | `{高 / 中 / 低}` | `{高 / 中 / 低}` | `{迁移脚本验证、备份}` | `{姓名}` |

---

## 5. 回滚方案

> 若变更实施后出现严重问题，需明确可执行的回滚路径。

| 回滚场景 | 触发条件 | 回滚步骤 | 预计耗时 | 负责人 |
|----------|----------|----------|----------|--------|
| `{场景 1：接口变更导致核心功能不可用}` | `{错误率 > X%}` | `{切换回旧版本 / 关闭 feature flag}` | `{X 分钟}` | `{姓名}` |
| `{场景 2：数据迁移异常}` | `{校验失败 / 数据不一致}` | `{执行逆向 migration / 从备份恢复}` | `{X 分钟}` | `{姓名}` |

**回滚限制**：`{如：schema 删除字段后无法无损回滚，需要提前备份}`

**回滚验证**：`{健康检查、核心链路验证、数据一致性校验}`

---

## 6. 相关文档更新清单

> 列出所有因本次变更需要新建、更新或废弃的文档，确保基线一致。

| 文档 | 动作 | 当前版本 | 变更后版本 | 负责人 |
|------|------|----------|------------|--------|
| `docs/PRD-vX.Y.Z.md` | `{更新 / 无需更新}` | `vX.Y.Z` | `vX.Y.Z+1` | `{姓名}` |
| `docs/TDD-vX.Y.Z.md` | `{更新 / 无需更新}` | `vX.Y.Z` | `vX.Y.Z+1` | `{姓名}` |
| `docs/API-SPEC-vX.Y.Z.md` | `{更新 / 无需更新}` | `vX.Y.Z` | `vX.Y.Z+1` | `{姓名}` |
| `docs/QA-TEST-PLAN-vX.Y.Z.md` | `{更新 / 无需更新}` | `vX.Y.Z` | `vX.Y.Z+1` | `{姓名}` |
| `docs/SECURITY-TEST-PLAN-vX.Y.Z.md` | `{更新 / 无需更新}` | `vX.Y.Z` | `vX.Y.Z+1` | `{姓名}` |

---

## 7. 审批记录

> 变更请求须经产品、技术、测试负责人评审，并留痕。

| 评审轮次 | 评审日期 | 评审人 | 角色 | 结论 | 意见 / 待办 |
|----------|----------|--------|------|------|-------------|
| `{初审}` | `{YYYY-MM-DD}` | `{姓名}` | `{产品负责人}` | `{通过 / 有条件通过 / 驳回}` | `{意见}` |
| `{复审}` | `{YYYY-MM-DD}` | `{姓名}` | `{技术负责人}` | `{通过 / 有条件通过 / 驳回}` | `{意见}` |
| `{终审}` | `{YYYY-MM-DD}` | `{姓名}` | `{测试负责人}` | `{通过 / 有条件通过 / 驳回}` | `{意见}` |

**最终审批结论**：`{已批准 / 已拒绝}`

**审批人签字**：`{姓名} · {日期}`

---

## 8. 实施后的验证与闭环

> 变更实施完成后，须验证预期收益是否达成，并更新相关文档与基线。

| 验证项 | 验证方式 | 通过标准 | 实际结果 | 负责人 |
|--------|----------|----------|----------|--------|
| `{功能正确性}` | `{QA 测试 / 自动化用例}` | `{AC-xxx 全部通过}` | `{结果}` | `{姓名}` |
| `{性能影响}` | `{压测 / 监控}` | `{延迟 ≤ X ms}` | `{结果}` | `{姓名}` |
| `{用户影响}` | `{埋点 / 客服反馈}` | `{无异常客诉}` | `{结果}` | `{姓名}` |
| `{文档更新}` | `{文档 diff 检查}` | `{基线文档已更新}` | `{结果}` | `{姓名}` |

**变更闭环结论**：`{已达成 / 未达成 / 需二次变更}`

**后续跟进事项**：`{如仍需补充的优化、遗留问题}`

---

## 9. 下游文档闭环更新

变更批准后、实施前，必须明确需同步更新的下游文档，并在变更关闭时完成闭环：

| 下游文档 | 是否需更新 | 更新内容摘要 | 负责人 | 状态 |
|----------|------------|--------------|--------|------|
| `docs/PRD-vX.Y.Z.md` | {是 / 否} | {需求变更说明} | {姓名} | {待办 / 已完成 / 不适用} |
| `docs/TDD-vX.Y.Z.md` | {是 / 否} | {设计变更说明} | {姓名} | {待办 / 已完成 / 不适用} |
| `docs/API-SPEC-vX.Y.Z.md` / `openapi-vX.Y.Z.yaml` | {是 / 否} | {接口变更说明} | {姓名} | {待办 / 已完成 / 不适用} |
| `docs/DATABASE-MODEL-vX.Y.Z.md` | {是 / 否} | {schema/索引变更} | {姓名} | {待办 / 已完成 / 不适用} |
| `docs/RELEASE-NOTES-vX.Y.Z.md` | {是 / 否} | {发布说明} | {姓名} | {待办 / 已完成 / 不适用} |
| `docs/CHANGELOG-vX.Y.Z.md` | {是 / 否} | {变更日志条目} | {姓名} | {待办 / 已完成 / 不适用} |
| `docs/RUNBOOK-vX.Y.Z.md` | {是 / 否} | {运维/监控更新} | {姓名} | {待办 / 已完成 / 不适用} |

---

## 10. 检查清单

- [ ] 变更背景与动机已清晰，且已说明不变更的影响
- [ ] 变更范围已明确区分“包含”与“不包含”
- [ ] 至少提供 2 个可选方案并完成对比
- [ ] 已评估成本、进度与风险影响
- [ ] 回滚方案可执行，且已识别回滚限制
- [ ] 下游文档闭环更新已评估并执行
- [ ] 相关文档更新清单已列出并指定负责人
- [ ] 审批记录完整，最终结论明确
- [ ] 实施后验证项与通过标准已定义
- [ ] 所有 `{占位符}` 已替换为真实内容
- [ ] 文档中无未清理的 `TODO` / `FIXME` 标记
